[tools]
lazygit = "latest"
neovim = "latest"
uv = "latest"

# Markdown Formatter
[tasks.lint]
run = "uvx mdformat $(find . -maxdepth 3 -name '*.md' ! -name 'README.md')"

[tasks.setup]
run = "uvx pre-commit install"
